apiVersion: v1
kind: Service
metadata:
  name: javaapp-service
  namespace: prod-namespace
spec:
  type: LoadBalancer
  selector:
    app: javaapp
  ports:
  - port: 1101
    targetPort: 80
    nodePort: 31999
