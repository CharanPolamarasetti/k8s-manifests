apiVersion: v1
kind: ResourceQuota
metadata:
  name: javaapp-quota
  namespace: prod-namespace
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: "512Mi"
    limits.cpu: "1"
    limits.memory: "1Gi"